create or replace TABLE MIRRORING.DIM_TRA_TIPOVEHICULO (
TIPOVEHICULO_ID	VARCHAR(18) PRIMARY KEY,
TIPOVEHICULO_TEXT	VARCHAR(60),
TIPOVEHICULO_ID_TEXT	VARCHAR(85),
SISORIGEN_ID	VARCHAR(3),
MANDANTE	VARCHAR(4),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(12)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_TRA_TIPOVEHICULO ON TABLE MIRRORING.DIM_TRA_TIPOVEHICULO;


    DELETE FROM MIRRORING.DIM_TRA_TIPOVEHICULO;
    INSERT INTO MIRRORING.DIM_TRA_TIPOVEHICULO
    (
        TIPOVEHICULO_ID,
        TIPOVEHICULO_TEXT,
        TIPOVEHICULO_ID_TEXT,
        SISORIGEN_ID,
        MANDANTE,
        FECHA_CARGA,
        ZONA_HORARIA
    )
    SELECT 
        TIPOVEHICULO_ID,
        TIPOVEHICULO_TEXT,
        CONCAT(TIPOVEHICULO_ID, ' - ', TIPOVEHICULO_TEXT) TIPOVEHICULO_ID_TEXT,
        SISORIGEN_ID,
        MANDANTE,
        FECHA_CARGA,
        ZONA_HORARIA
    FROM  
        CON.DIM_TRA_TIPOVEHICULO;


    SELECT * FROM MIRRORING.DIM_TRA_TIPOVEHICULO;