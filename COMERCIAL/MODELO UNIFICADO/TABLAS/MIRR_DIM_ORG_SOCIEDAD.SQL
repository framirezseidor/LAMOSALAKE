CREATE OR ALTER TABLE MIRRORING.DIM_ORG_SOCIEDAD (
    SOCIEDAD_ID VARCHAR(4) NOT NULL,
    PAIS_ID VARCHAR(3) NOT NULL,
    NEGOCIOGRUPO_ID VARCHAR(4) NOT NULL,
    SOCIEDAD_TEXT VARCHAR(25) NOT NULL,
    SOCIEDAD_ID_TEXT VARCHAR(40) NOT NULL,    
    PAIS_TEXT VARCHAR(25) NOT NULL,
    NEGOCIOGRUPO_TEXT VARCHAR(25) NOT NULL,
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,

    CONSTRAINT PK_DIM_ORG_SOCIEDAD PRIMARY KEY (SOCIEDAD_ID)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_ORG_SOCIEDAD ON TABLE MIRRORING.DIM_ORG_SOCIEDAD;