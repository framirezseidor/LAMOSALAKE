
CREATE OR ALTER TABLE MIRRORING.DIM_CLI_ASESORCOMERCIAL (
    ASESORCOMERCIAL_ID VARCHAR(50),
    FUNCION_INTERLOCUTOR VARCHAR(50),
    ASESORCOMERCIAL_TEXT VARCHAR(100),
    ASESORCOMERCIAL_ID_TEXT VARCHAR(100),    
    SISORIGEN_ID VARCHAR(4),
    MANDANTE VARCHAR(4),
    FECHA_CARGA TIMESTAMP_TZ,
    ZONA_HORARIA VARCHAR(10)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_CLI_ASESORCOMERCIAL ON TABLE MIRRORING.DIM_CLI_ASESORCOMERCIAL;