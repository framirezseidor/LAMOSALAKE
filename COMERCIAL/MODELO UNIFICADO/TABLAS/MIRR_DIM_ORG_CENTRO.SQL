create or replace TABLE MIRRORING.DIM_ORG_CENTRO (
    CENTRO_ID VARCHAR(4) NOT NULL,
    ORGVENTAS_ID VARCHAR(4) NOT NULL,
    PAIS_ID VARCHAR(3) NOT NULL,
    REGION_ID VARCHAR(7) NOT NULL,
    CENTRO_TEXT VARCHAR(30) NOT NULL,
    CENTRO_ID_TEXT VARCHAR(40) NOT NULL,
    ORGVENTAS_TEXT VARCHAR(40) NOT NULL,
    PAIS_TEXT VARCHAR(30) NOT NULL,
    REGION_TEXT VARCHAR(40) NOT NULL,
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,

    CONSTRAINT PK_DIM_ORG_CENTRO PRIMARY KEY (CENTRO_ID)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_ORG_CENTRO ON TABLE MIRRORING.DIM_ORG_CENTRO;