create or replace TABLE MIRRORING.DIM_ORG_ZONAVENTAS (
    ZONAVENTAS_ID VARCHAR(6) NOT NULL,
    ZONAVENTAS_TEXT VARCHAR(20) NOT NULL,
    ZONAVENTAS_ID_TEXT VARCHAR(30) NOT NULL,
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,

    CONSTRAINT PK_DIM_ORG_ZONAVENTAS PRIMARY KEY (ZONAVENTAS_ID)
);


CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_ORG_ZONAVENTAS ON TABLE MIRRORING.DIM_ORG_ZONAVENTAS;