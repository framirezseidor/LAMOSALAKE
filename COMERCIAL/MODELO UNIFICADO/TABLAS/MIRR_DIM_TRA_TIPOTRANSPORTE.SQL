create or replace TABLE MIRRORING.DIM_TRA_TIPOTRANSPORTE (
TIPOTRANSPORTE_ID	VARCHAR(10) PRIMARY KEY,
TIPOTRANSPORTE_TEXT	VARCHAR(60),
TIPOTRANSPORTE_ID_TEXT	VARCHAR(75),
SISORIGEN_ID	VARCHAR(5),
MANDANTE	VARCHAR(4),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(12)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_TRA_TIPOTRANSPORTE ON TABLE MIRRORING.DIM_TRA_TIPOTRANSPORTE;