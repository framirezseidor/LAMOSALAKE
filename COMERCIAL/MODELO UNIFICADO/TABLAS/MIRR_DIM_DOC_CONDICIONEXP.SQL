create or replace TABLE MIRRORING.DIM_DOC_CONDICIONEXP (
CONDICIONEXP_ID	VARCHAR(50) PRIMARY KEY,
CONDICIONEXP_TEXT	VARCHAR(100),
CONDICIONEXP_ID_TEXT	VARCHAR(100),
SISORIGEN_ID	VARCHAR(10),
MANDANTE	VARCHAR(10),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(10)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_DOC_CONDICIONEXP ON TABLE MIRRORING.DIM_DOC_CONDICIONEXP;
