create or replace TABLE MIRRORING.DIM_OBR_PROMOTOR (
	PROMOTOR_ID VARCHAR(10) NOT NULL,
    FUNCION_INTERLOCUTOR VARCHAR(30) NOT NULL,
    PROMOTOR_TEXT VARCHAR(35) NOT NULL,
    PROMOTOR_ID_TEXT VARCHAR(50) NOT NULL,   
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,

    CONSTRAINT PK_DIM_OBR_PROMOTOR PRIMARY KEY (PROMOTOR_ID,FUNCION_INTERLOCUTOR)

);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_OBR_PROMOTOR ON TABLE MIRRORING.DIM_OBR_PROMOTOR;