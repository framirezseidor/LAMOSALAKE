CREATE OR ALTER TABLE MIRRORING.DIM_TRA_PUESTOPLANTRANSP (
PUESTOPLANTRANSP_ID	VARCHAR(4) PRIMARY KEY,
PUESTOPLANTRANSP_TEXT	VARCHAR(20),
PUESTOPLANTRANSP_ID_TEXT	VARCHAR(30),
SISORIGEN_ID	VARCHAR(3),
MANDANTE	VARCHAR(4),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(12)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_TRA_PUESTOPLANTRANSP ON TABLE MIRRORING.DIM_TRA_PUESTOPLANTRANSP;