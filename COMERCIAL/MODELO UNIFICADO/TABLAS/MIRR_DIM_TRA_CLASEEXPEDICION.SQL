create or replace TABLE MIRRORING.DIM_TRA_CLASEEXPEDICION (
CLASEEXPEDICION_ID	VARCHAR(3) PRIMARY KEY,
CLASEEXPEDICION_TEXT	VARCHAR(40),
CLASEEXPEDICION_ID_TEXT	VARCHAR(50),
SISORIGEN_ID	VARCHAR(3),
MANDANTE	VARCHAR(4),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(12)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_TRA_CLASEEXPEDICION ON TABLE MIRRORING.DIM_TRA_CLASEEXPEDICION;