CREATE OR ALTER TABLE MIRRORING.DIM_TRA_TRANSPORTISTA (
TRANSPORTISTA_ID	VARCHAR(10) PRIMARY KEY,
TRANSPORTISTA_TEXT	VARCHAR(35),
TRANSPORTISTA_ID_TEXT	VARCHAR(50),
SISORIGEN_ID	VARCHAR(3),
MANDANTE	VARCHAR(4),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(12)
);

CREATE OR REPLACE STREAM  MIRRORING.STREAM_DIM_TRA_TRANSPORTISTA ON TABLE  MIRRORING.DIM_TRA_TRANSPORTISTA;