CREATE OR ALTER TABLE MIRRORING.DIM_TRA_RUTA (
RUTA_ID	VARCHAR(6) PRIMARY KEY,
RUTA_TEXT	VARCHAR(40),
RUTA_ID_TEXT	VARCHAR(50),
SISORIGEN_ID	VARCHAR(4),
MANDANTE	VARCHAR(3),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(12)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_TRA_RUTA ON TABLE MIRRORING.DIM_TRA_RUTA;