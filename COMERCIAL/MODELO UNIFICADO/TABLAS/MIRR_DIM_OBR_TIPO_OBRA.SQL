CREATE OR ALTER TABLE MIRRORING.DIM_OBR_TIPO_OBRA (
	TIPO_OBRA_ID VARCHAR(3) NOT NULL,
    TIPO_OBRA_TEXT VARCHAR(40) NOT NULL,
    TIPO_OBRA_ID_TEXT VARCHAR(50) NOT NULL,   
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,

    CONSTRAINT PK_DIM_OBR_TIPO_OBRA PRIMARY KEY (TIPO_OBRA_ID)

);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_OBR_TIPO_OBRA ON TABLE MIRRORING.DIM_OBR_TIPO_OBRA;