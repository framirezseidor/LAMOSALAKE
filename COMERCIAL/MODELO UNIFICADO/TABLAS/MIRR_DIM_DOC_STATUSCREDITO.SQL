DROP TABLE MIRRORING.DIM_DOC_STATUSCREDITO ;
create or replace TABLE MIRRORING.DIM_DOC_STATUSCREDITO (
STATUSCREDITO_ID	VARCHAR(50) PRIMARY KEY,
STATUSCREDITO_TEXT	VARCHAR(100),
STATUSCREDITO_ID_TEXT	VARCHAR(100),
SISORIGEN_ID	VARCHAR,
MANDANTE	VARCHAR,
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_DOC_STATUSCREDITO ON TABLE MIRRORING.DIM_DOC_STATUSCREDITO ;

