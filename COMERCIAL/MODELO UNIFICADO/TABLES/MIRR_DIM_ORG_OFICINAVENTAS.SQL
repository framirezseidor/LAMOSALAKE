create or replace TABLE MIRRORING.DIM_ORG_OFICINAVENTAS (
    OFICINAVENTAS_ID VARCHAR(4) NOT NULL,
    OFICINAVENTAS_TEXT VARCHAR(20) NOT NULL,
    OFICINAVENTAS_ID_TEXT VARCHAR(30) NOT NULL,    
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,

    CONSTRAINT PK_DIM_ORG_OFICINAVENTAS PRIMARY KEY (OFICINAVENTAS_ID)
);


CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_ORG_OFICINAVENTAS ON TABLE MIRRORING.DIM_ORG_OFICINAVENTAS;