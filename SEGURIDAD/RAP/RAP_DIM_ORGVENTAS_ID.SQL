CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_ORGVENTAS_ID
AS (ORGVENTAS_ID VARCHAR(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(ORGVENTAS_ID , 'ORGVENTAS_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;