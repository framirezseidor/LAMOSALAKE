CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_SOCIEDAD_ID
AS (SOCIEDAD_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(SOCIEDAD_ID , 'SOCIEDAD_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;