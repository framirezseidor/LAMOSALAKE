create or replace TABLE MIRRORING.DIM_ORG_SECTOR (
    SECTOR_ID VARCHAR(2) NOT NULL,
    SECTOR_TEXT VARCHAR(20) NOT NULL,
    SECTOR_ID_TEXT VARCHAR(30) NOT NULL,    
    SISORIGEN_ID VARCHAR(4) NOT NULL,
	MANDANTE VARCHAR(4) NOT NULL,
	FECHA_CARGA TIMESTAMP_TZ(9) NOT NULL,
	ZONA_HORARIA VARCHAR(10) NOT NULL,
    CONSTRAINT PK_DIM_ORG_SECTOR PRIMARY KEY (SECTOR_ID)
);

CREATE OR REPLACE STREAM MIRRORING._STREAM_DIM_ORG_SECTOR ON TABLE MIRRORING.DIM_ORG_SECTOR;