create or replace TABLE MIRRORING.DIM_DOC_INCOTERMS (
INCOTERMS_ID	VARCHAR(50) PRIMARY KEY,
INCOTERMS_TEXT	VARCHAR(100),
INCOTERMS_ID_TEXT	VARCHAR(100),
SISORIGEN_ID	VARCHAR(10),
MANDANTE	VARCHAR(10),
FECHA_CARGA	TIMESTAMP_TZ,
ZONA_HORARIA	VARCHAR(10)
);

CREATE OR REPLACE STREAM MIRRORING.STREAM_DIM_DOC_INCOTERMS ON TABLE MIRRORING.DIM_DOC_INCOTERMS;