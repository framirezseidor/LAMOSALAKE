CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_CANALDISTRIB_ID
AS (CANALDISTRIB_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(CANALDISTRIB_ID , 'CANALDISTRIB_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;