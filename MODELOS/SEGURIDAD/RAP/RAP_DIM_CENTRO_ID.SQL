CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_CENTRO_ID
AS (CENTRO_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(CENTRO_ID , 'CENTRO_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;