CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_OFICINAVENTAS_ID
AS (OFICINAVENTAS_ID VARCHAR(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(OFICINAVENTAS_ID , 'OFICINAVENTAS_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;