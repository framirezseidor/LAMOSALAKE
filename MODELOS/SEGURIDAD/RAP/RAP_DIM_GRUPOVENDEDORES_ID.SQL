CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_GRUPOVENDEDORES_ID
AS (GRUPOVENDEDORES_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(GRUPOVENDEDORES_ID , 'GRUPOVENDEDORES_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;