CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_ORGCOMPRAS_ID
AS (ORGCOMPRAS_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(ORGCOMPRAS_ID , 'ORGCOMPRAS_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;

