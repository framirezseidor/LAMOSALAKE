CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_SOLICITANTE_ID
AS (GRUPOCLIENTES2_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(GRUPOCLIENTES2_ID , 'GRUPOCLIENTES2_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;

