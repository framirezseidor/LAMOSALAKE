CREATE OR REPLACE ROW ACCESS POLICY SEGURIDAD.RAP_SAP_DIM_PAIS_ID
AS (PAIS_ID varchar(50))
RETURNS BOOLEAN ->
CASE
    WHEN (SELECT SEGURIDAD.FN_RAP_DIM(PAIS_ID , 'PAIS_ID'))=TRUE
    THEN TRUE
ELSE
    FALSE
END
;

 