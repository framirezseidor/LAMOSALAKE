CREATE OR REPLACE FUNCTION SEGURIDAD.FN_VALIDA_SAP_ROLE()
RETURNS BOOLEAN
LANGUAGE SQL
AS
$$
    SELECT EXISTS(
        SELECT 1 
        FROM SEGURIDAD.SEG_USUARIO_ROLE
        WHERE USER_NM = CURRENT_USER 
        AND role_nm = 'SAP_ROLE'
    )
$$