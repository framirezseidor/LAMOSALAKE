CREATE OR REPLACE FUNCTION SEGURIDAD.FN_VALIDA_USUARIO_DIM(NOMBRE_DIM VARCHAR(50))
RETURNS BOOLEAN
LANGUAGE SQL
AS 
$$
    SELECT EXISTS(
        SELECT 1 
        FROM SEGURIDAD.SEG_USUARIO_DIMENSION 
        WHERE USER_NM = CURRENT_USER 
        AND DIM = NOMBRE_DIM) 
$$