CREATE OR REPLACE VIEW CON.VW_DIM_FIN_VERSION(
    VERSION_ID,
    VERSION_TEXT,
    SISORIGEN_ID,
    MANDANTE,
    FECHA_CARGA,
    ZONA_HORARIA
) AS

SELECT  VERSION_ID,
        VERSION_TEXT,
        SISORIGEN_ID,
        MANDANTE,
        FECHA_CARGA,
        ZONA_HORARIA
FROM CON.DIM_FIN_VERSION;